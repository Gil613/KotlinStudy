- 고차함수(High-order function)
    - 함수를 인자로 받고 결과 값으로 내보낼 수 있다.
    fun function1(number: Int) -> Int = {}
    - 함수 타입 표시하는 방법
        - (파라미터1, 파라미터2) -> 리턴 자료형
        fun function2(function : (Int, Int) -> Int){
            함수내용
        }
    - 고차함수 호출 방법
        - function1(100, 100) -> 일반적인 함수 호출 방법
        - function2(::function1)

  - 일급객체 (First Class Citizen) 일급함수 일급시민....
     - 무엇이든 할 수 있다 ~ 어디든 갈 수 있다
         -> 이름이 없어도 된다. 반환 값이 될 수 있다, 함수에 인자가 될 수 있다.

- 람다(Lambda)
    - 람다함수는 그 자체로 고차 함수
    - 파라미터가 한 개인 경우에는 it사용
    - 이름을 가질 필요가 없다
    - 람다에서는 return이 필요 없다 마지막 실행 코드가 리턴이 된다.
    - 코드가 간결해진다.
    - 지연연산을 수행하여 불필요 연산을 최소화 한다
    - 멀티쓰레드를 활용하여 병렬처리가 가능
    - 호출이 까다롭다
    - 불필요하게 사용하면 오히려 가독성이 떨어진다.



